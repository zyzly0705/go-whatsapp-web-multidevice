name: Build WA Alert

on:
  workflow_dispatch:
  push:
    branches:
      - "codex/**"

jobs:
  build:
    name: Build macOS and Windows binaries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Run tests
        working-directory: src
        run: go test ./...

      - name: Build local browser binaries
        working-directory: src
        run: ./scripts/build-local-browser.sh

      - name: Upload macOS and Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wa-alert-macos-windows
          path: |
            src/dist/wa-alert-darwin-amd64
            src/dist/wa-alert-darwin-arm64
            src/dist/wa-alert-windows-amd64.exe
          if-no-files-found: error
